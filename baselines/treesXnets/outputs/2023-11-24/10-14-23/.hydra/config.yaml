num_clients: 3
batch_size: 64
device: cuda:0
task: None
dataset:
  name: imodels/credit-card
  partition: linear
  id_col: None
  num_clients: ${num_clients}
  val_frac: 0.0
  num_input: 0
  num_classes: 0
model:
  _target_: treesXnets.models.mlp.MLP
  model_size: small
  input_dim: ${dataset.num_input}
  output_dim: ${dataset.num_classes}
  task: ${task}
  batch_size: ${batch_size}
strategy:
  _target_: null
client_fn:
  _target_: treesXnets.client.gen_client_fn
client:
  batch_size: 64
